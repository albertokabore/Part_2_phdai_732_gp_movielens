# Surprise compiles against NumPy at install time. If `pip install` fails or
# `import surprise` raises a NumPy ABI error, see README, "Setup".
scikit-surprise>=1.1.4
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
# Lets the downloader use Windows/macOS certificate verification (see src/data.py).
truststore>=0.9; python_version >= "3.10"
